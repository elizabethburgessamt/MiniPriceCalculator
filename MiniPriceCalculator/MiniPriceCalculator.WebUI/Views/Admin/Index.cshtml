@model IEnumerable<MiniPriceCalculator.Domain.Entities.Product>

@{
    ViewBag.Title = "Admin: Order Management";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="panel-heading">
    <h3>Order Management</h3>
</div>

<div class="panel-body">
    <div class="container">
        <ul class="nav nav-tabs">
            <li class="active"><a data-toggle="tab" href="#order">Pending Order</a></li>
            <li><a data-toggle="tab" href="#done">Completed Orders</a></li>
            <li><a data-toggle="tab" href="#statistics">Statistics</a></li>
        </ul>

        <div class="tab-content">
            <div id="order" class="tab-pane fade in active">
                <table class="table table-striped table-condensed table-bordered">
                    <tr>
                        <th>ID</th>
                        <th>Customer Name</th>
                        <th>DateCreated</th>
                        <th>Quantity</th>
                        <th>Address</th>
                    </tr>
                    @*@foreach (var item in Model)
                    {
                        <tr>
                            <td class="text-right">@item.ProductID</td>
                            <td>@Html.ActionLink(item.Name, "Edit", new { item.ProductID })</td>
                            <td class="text-right">@item.Price.ToString("c")</td>
                            <td class="text-center">
                                @using (Html.BeginForm("Delete", "Admin"))
                                {
                                    @Html.Hidden("ProductID", item.ProductID)
                                    <input type="submit" class="btn btn-default btn-xs" value="Delete" />
                                }
                            </td>
                        </tr>
                    }*@
                </table>
            </div>
            <div id="done" class="tab-pane fade">
                <table class="table table-striped table-condensed table-bordered">
                    <tr>
                        <th>ID</th>
                        <th>Customer Name</th>
                        <th>DateCompleted</th>
                        <th>Quantity</th>
                    </tr>
                </table>
            </div>
            <div id="statistics" class="tab-pane fade">
                Placeholder for neat statistics models
            </div>
        </div>
    </div>
    
</div>
